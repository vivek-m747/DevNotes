# Services package — Business logic layer.
# Contains validation, authorization, and orchestration logic.
# Services call repositories for database access — they never
# import SQLAlchemy or write queries directly.